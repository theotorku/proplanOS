# Core API framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Data validation
pydantic>=2.0.0
# Required by pydantic EmailStr — used by the chat capture_lead / book_call bodies
email-validator>=2.0.0

# Anthropic integration (optional — required for real LLM planning)
anthropic>=0.49.0

# Supabase integration (optional — required for production persistence)
supabase>=2.4.0

# Async task queuing (optional — required for /agent/run/async)
celery>=5.3.0
redis>=5.0.0

# Testing
pytest>=8.0.0
httpx>=0.27.0

